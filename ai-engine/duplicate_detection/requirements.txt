# Duplicate Detection Module Requirements
# ========================================
# GIIPS AI Engine - Duplicate Detection
#
# Runtime dependencies come from ai-engine/backend/requirements.txt
# (fastembed provides the ONNX semantic encoder — no torch needed).
# This file is informational; install with:
#   pip install fastembed

# ONNX semantic embeddings (default duplicate-detection path)
fastembed>=0.8.0

# Numerical processing
numpy>=1.24.0
